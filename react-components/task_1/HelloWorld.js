/*n the code editor, we’ve provided three files, App.js, index.js, and HelloWorld.js. Render a “Hello World!” greeting onto the screen with the appropriate file structure, where App.js contains the top-level component and index.js renders it.*/
import React from 'react';

function HelloWorld() {
    return (
      <h1>Hello World</h1>
    );
};

export default HelloWorld;